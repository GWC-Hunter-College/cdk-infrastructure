queries for admin of event manager (verifies clubs to club lists, this is so 
a random person doesnt make some funky ahh clubs)

put the query of:

GET /admins/{studentId}
DELETE /admins/{studentId}

in this folder